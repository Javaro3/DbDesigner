version: '3.8'

services:
  sqlite:
    image: alpine:latest
    container_name: dbdesigner_sqlite
    volumes:
      - ../../../../mounts/SqLite:/data
    command: sh -c "apk add --no-cache sqlite && sleep infinity"