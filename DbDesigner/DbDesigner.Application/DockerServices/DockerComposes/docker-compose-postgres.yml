version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: dbdesigner_postgres
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
    ports:
      - "5432:5432"
    volumes:
      - ../../../../mounts/PostgreSql:/var/lib/postgresql/data