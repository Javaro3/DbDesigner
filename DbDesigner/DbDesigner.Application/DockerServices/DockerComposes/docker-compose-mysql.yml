version: '3.8'

services:
  mysql:
    image: mysql:latest
    container_name: dbdesigner_mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - ../../../../mounts/MySql:/var/lib/mysql